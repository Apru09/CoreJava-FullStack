public class TerenaryOperator {
    
    public static void main(String[] args) {

        /* Terenary operator is used as a simple "if else" */
        
        int number = 10;

        String result = (number % 2 == 0) ? "Even" : "Odd";

        System.out.println(result);
    }
}
